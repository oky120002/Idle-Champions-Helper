# 全站正则表达式正确性审查

**状态**: 已落地
**类型**: change
**范围**: `src/` 与 `scripts/data/`
**创建日期**: 2026-08-14
**来源**: TODO `atd_regex_audit_001`

## 目标

核对所有正则表达式的语法、边界、复杂度和业务契约；对已确认的行为缺陷补测试并做最小修复，对无缺陷的使用点留下可复核的审计结论。

## 范围

- `src/`：搜索高亮、导入清洗、领域谓词、效果载荷及其他运行时代码。
- `scripts/data/`：限制文本解析、搜索索引、数据归一化和官方规则辅助逻辑。
- 真实产物样本、调用方和现有测试作为正则语义的验证依据。
- 不扩大到正则以外的独立数据缺口；新发现的问题按 `auto-todo` 单独记录。

## 阶段 Checklist

- [x] 阶段 1：完成 AST 盘点与调用链分类 —— 验证方式：盘点结果覆盖 `src/`、`scripts/data/` 的正则字面量和动态构造。
- [x] 阶段 2：完成业务契约审查 —— 验证方式：每个使用点归类为已验证、已修复缺陷或静态/理论风险，并保留文件位置与证据。
- [x] 阶段 3：补充回归测试并修复确认缺陷 —— 验证方式：新增测试先失败后通过，且不削弱原有断言。
- [x] 阶段 4：同步文档与 TODO —— 验证方式：当前态规范/审计文档反映实现，已完成条目从 canonical TODO 删除。
- [x] 阶段 5：完成最小充分验证 —— 验证方式：类型检查、lint、正则回归测试、文档治理测试、构建和 `git diff --check` 通过；全量测试另有 4 个与本审计无关的既有 UI/治理失败，已记录独立 TODO。

## 验收

- 正则审查范围和结论可由仓库内文档与代码证据复核。
- 已确认缺陷有回归测试和最小实现修复。
- 无未解释的正则行为变化；未解决的独立问题有单独 TODO 记录。
- 主目标完成后，计划标记为已落地并按治理规则归档。

## 落地后

- `specs/` 更新点：按最终实现更新受影响模块的当前规则；审计明细保留在 `docs/audits/`。
- 本 change 状态 → 已落地 → 移 `archives/plans/`
- **specs/ 永不引用本 change**（规范描述最终态，不描述交付过程）
