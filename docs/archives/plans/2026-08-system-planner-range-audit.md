# Planner 变更范围深度审查

**状态**: 已落地
**类型**: milestone
**范围**: system
**创建日期**: 2026-08-15

## 目标

审查并收口 `59f9d129..2d583211` 的代码、数据与文档变更，修复会影响最佳阵型推算正确性、可解释性或数据契约的根因，并留下可重复验证证据。

## 范围

- restriction 文本解析与场景模型构建。
- 场景产物、用户快照与消息引用的运行时 schema、归一化和消费链路。
- planner 推荐、伤害来源位置规则及页面展示。
- 真实公共场景产物、测试、规范与 TODO 的一致性。

## 阶段 Checklist

- [x] 阶段 1：冻结工作树、规则、CodeGraph 与提交范围 —— 验证方式：Git、CodeGraph 和差异清单可复现。
- [x] 阶段 2：审查代码、数据、文档和调用链 —— 验证方式：逐项记录代码证据与真实产物对比。
- [x] 阶段 3：实施高价值根因修复并记录低价值项 —— 验证方式：co-located 测试或数据守护测试覆盖新增行为。
- [x] 阶段 4：执行类型、单元、数据、构建及文档治理验证 —— 验证方式：命令成功或明确记录环境阻塞。
- [x] 阶段 5：同步 specs/审计文档、完成 Git 提交并复核工作树 —— 验证方式：提交内容、验证结果和剩余风险可追溯。

## 验收

- 本范围内的高风险正确性、数据契约、隐私/安全和可用性问题已修复，或有明确证据证明无需修复。
- 真实产物能通过对应 schema，planner 关键链路有测试覆盖，文档描述与当前代码一致。
- 低价值但有效的后续项进入根 `TODO.md` 的 canonical auto-todo 区块。
- 最小充分验证完成，工作树只包含本任务变更并形成一个完整提交。

## 落地后

- specs/ 更新点：
  - `docs/specs/modules/planner/`：同步 planner 当前场景约束、消息引用和验证边界。
  - `docs/specs/guidelines/testing.md`：仅在发现测试门控或守护规则需要稳定化时更新。
- 必要时新增 `docs/audits/` 审计快照，记录范围、证据、验证和剩余风险。
- 本 change 状态 → 已落地 → 移 `archives/plans/`
- **specs/ 永不引用本 milestone**（规范描述最终态，不描述交付过程）。

