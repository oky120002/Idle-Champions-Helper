# per-hero 叠层默认值

**优先级**：待评

## 是什么

叠层初始值（pre_stack）当前统一默认 1000（满层），可引入 per-hero per-mechanism 合理默认。

## 为何暂缓

体验/默认值缺口，非正确性；UI 已可按冒险区域手调。逐英雄逐机制定默认数据量大、收益不明。

## 关联

- [data-blindspot-audit.md §3.4](../audits/data-blindspot-audit.md)
