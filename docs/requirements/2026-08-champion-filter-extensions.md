# 英雄筛选扩展

**优先级**：待评

## 是什么

英雄筛选模块的 MVP 补全与扩展。下为各子项当前落地状态（2026-08-06 核验）。

### Checklist

- [x] **Patron 过滤** + 结果解释（2026-08-06 落地，证据：`2f80050d` + `bf1090b0`）—— 英雄页 / 用户英雄页 / 插图页高频条件组新增赞助人筛选项，按 `eligiblePatronIds` 命中（多选 OR），active chip 可单独清除
- [ ] **目标模式过滤**（模式维度缩小候选池）
- [ ] **结构化规则数据集合**（筛选结果和不可用原因消费同一规则来源）
- [x] **过滤标签扩展**（2026-08-07 落地）：补齐 59 个缺失标签翻译（24 种族 + 18 联动队伍 + 17 机制/定位），中文环境不再显示英文 key（`src/domain/champion-tags/tag-labels/`）
- [ ] **个人画像接入**（owned / unowned、已解锁 / 未解锁标记）
- [ ] **细场景规则**（Variant 规则、冒险 / 变体上下文联动）
- [ ] **可解释模板推算**（只做可解释的规则推算，不做黑盒打分）

## 为何暂缓

当前筛选模块覆盖基础维度（seat、关键词、tag 派生组、特殊机制、赞助人），主体功能可用。上述未勾选项是渐进增强，按价值排序逐步推进。

## 关联

- `docs/specs/modules/champions/filter/rules.md`（当前筛选规则）
- `docs/specs/modules/champions/filter/scope-and-data.md`（当前数据范围）
