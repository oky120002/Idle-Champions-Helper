# 阵型编辑：规则扩展

**Status**: Draft
**Type**: change
**Scope**: formation
**Created**: 2026-07-27

## 目标

阵型编辑硬规则从单一 seat 冲突扩展到更完整的限制集合。

## 范围

- 禁用槽位
- 冒险 / 变体绑定布局
- 前后排提示
- 标签资格限制

## 阶段 Checklist

- [ ] 禁用槽位：定义规则 + 验证 —— 验证方式：单测覆盖禁用槽位不被填充
- [ ] 冒险/变体绑定布局：场景 → 布局映射 —— 验证方式：场景切换时布局正确切换
- [ ] 前后排提示 —— 验证方式：UI 显示前后排语义
- [ ] 标签资格限制 —— 验证方式：不符合标签的英雄不能放入

## 验收

每条规则下沉到规则层（不硬编码 JSX），有单测覆盖；UI 实时反映规则状态。

## 落地后

- `specs/modules/formation/rules.md`：新增规则条目
- 本 change Status → Landed → 移 `archive/changes/`
