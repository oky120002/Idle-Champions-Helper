# 本地运行与预览验证

- 日期：2026-04-13
- 作用：本页只做本地运行主题入口；细节已拆到 `docs/investigations/runtime/local-run/`。
- 当前结论：日常开发应使用 `npm run dev`；GitHub Pages 路径校验应使用 `npm run preview:pages`；直接访问 `npm run preview` 的根路径仍不能代表真实生产路径。

## 先读哪篇

- `build / preview / preview:pages / dev` 的实际结果：`docs/investigations/runtime/local-run/command-results.md`
- 当前页面形态、推荐入口、注意事项与依据：`docs/investigations/runtime/local-run/recommended-entry.md`
